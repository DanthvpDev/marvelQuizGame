export interface UserAuth {
    email: string;
    password: string;
}

export interface IUser {
    username: string;
    email:string,
} 

export interface IUserLoginInfo {
    username: string;
    email:string,
    password: string;
}